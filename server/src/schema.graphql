type Query {
  info: String!
  users: [User!]!
}

type User {
  id: ID!
  name: String!
  surname: String!
  email: String!
  url: String!
  interests: [Interest!]!
}

type Interest {
  id: ID!,
  text: String!
}
